from math import log
from math import floor


def twobit(i):
    # Base case
    if i == inp:
        return
    print(a)
    a[-i] = 1


inp = int(input("Enter the Number : "))
size = floor(log(inp, 2)) + 1
a = [0] * size
twobit(1)
